# Library Update Log

## 2026-09-03

* **Initialization**: created the initial OKF v0.2 `workspace/` bootstrap.
* **Restructure**: moved the OKF bundle root from `workspace/` to the repository root; source records now live in `sources/`, knowledge summaries in `docs/`, optional artifacts in `outputs/`.
* **Automation**: added the knowledge issue form, trusted-gate agent workflow, deterministic coordinator/validator/publisher scripts, and stage prompts for Pi.
